\chapter{EXPERIMENTAL RESULTS AND DISCUSSION}
\label{chap:experiments}

This chapter evaluates the proposed Vision-Language-Action (VLA) system on robot
manipulation tasks. The evaluation is organized around practical deployment goals:
end-to-end pipeline validity, instruction following, integration quality,
generalization, and runtime feasibility under a constrained setup.

To keep interpretation consistent with Chapter~\ref{chap:methodology}, results are
reported from system level to component level, then from seen-task performance to
transfer behavior. This ordering helps separate architectural gains from dataset-domain
effects.


%─────────────────────────────────────────────────────────────────────────────
\section{EXPERIMENTAL SETUP}
%─────────────────────────────────────────────────────────────────────────────

\subsection{Hardware and Runtime Environment}

All experiments are executed with GPU-accelerated inference in a commercial-resource
setting (targeting approximately 8 GB VRAM). Measurements include
end-to-end timing from input acquisition to action output and task-level performance
under both seen and unseen conditions.

The evaluation emphasizes reproducible operating conditions over peak-throughput
optimization. This is important because the thesis objective is practical deployability
under constrained hardware, not unrestricted-scale benchmarking.

\subsection{Task Protocol}

Evaluation focuses on four manipulation categories:
\begin{enumerate}[label=\arabic*)]
    \item Pick-and-place,
    \item Object stacking,
    \item Drawer/container interaction,
    \item Multi-step sequencing.
\end{enumerate}

Each category includes seen-task and unseen-task variants to test generalization.

\subsection{Evaluation Domains}

Experiments are divided into two evaluation domains and executed in two environments:
\begin{enumerate}[label=\arabic*)]
    \item \textbf{Success-rate domain}: standard task execution quality.
    \item \textbf{Zero-shot domain}: transfer performance without additional tuning.
\end{enumerate}

Each domain is tested:
\begin{itemize}
    \item on simulation,
    \item on real robot.
\end{itemize}

\subsection{Metrics}

The following metrics are used:
\begin{itemize}
    \item \textbf{Task success rate}: percentage of tasks completed according to
    natural-language intent.
    \item \textbf{Action validity}: percentage of generated actions that are executable
    by the robot controller.
    \item \textbf{Generalization score}: unseen-task performance relative to seen-task
    performance.
    \item \textbf{Latency}: median end-to-end command-cycle time.
\end{itemize}


%─────────────────────────────────────────────────────────────────────────────
\section{END-TO-END VLA PIPELINE PERFORMANCE}
%─────────────────────────────────────────────────────────────────────────────

The final system supports complete Vision-Language-Action processing in one pipeline.
Given an RGB observation and natural-language command, the model outputs structured
robot actions that can be executed without manual rule writing for each task.

This section first verifies that the pipeline is operationally sound before
interpreting higher-level behavior. In low-resource VLA development, this validation is
necessary because semantic accuracy is not meaningful if commands are not executable.

\begin{table}[ht]
\centering
\caption{System-level output quality for end-to-end VLA inference.}
\label{tab:vla_system_quality}
\begin{tabular}{lc}
\hline
\textbf{Measure} & \textbf{Result} \\
\hline
Executable action outputs & 95.8\% \\
Valid structured command format & 97.1\% \\
Task completion on benchmark set & 84.6\% \\
\hline
\end{tabular}
\end{table}

These results indicate that the VLA pipeline is functionally complete and suitable for
practical manipulation execution.

Notably, the gap between structured-command validity (97.1\%) and full task
completion (84.6\%) indicates that remaining errors are concentrated in semantic or
temporal decision quality rather than packet formatting failures.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{episode_0_task_redcubeplaceonpurple.png}
    \caption{Representative episode rollout for a pick-and-place instruction, showing joint trajectories and synchronized camera observations over time.}
    \label{fig:episode_rollout}
\end{figure}

Figure~\ref{fig:episode_rollout} shows a coherent control sequence: the gripper
state rises during the transport phase and returns to open near the end of the
episode, while both camera views confirm that the red cube is moved onto the
target location. This qualitative trajectory evidence is consistent with the high
executable-action and task-completion rates in Table~\ref{tab:vla_system_quality}.


%─────────────────────────────────────────────────────────────────────────────
\section{INTEGRATION OF VLM, VISION EXPERT, AND ACTION EXPERT}
%─────────────────────────────────────────────────────────────────────────────

Module ablation is used to evaluate integration value. The full system is compared
against reduced variants.

This analysis isolates whether gains come from a single strong module or from
coordinated cross-module interaction.

\begin{table}[ht]
\centering
\caption{Ablation results for module integration.}
\label{tab:integration_ablation}
\begin{tabular}{lcc}
\hline
\textbf{Configuration} & \textbf{Task Success (\%)} & \textbf{Action Validity (\%)} \\
\hline
VLM only & 68.9 & 81.5 \\
VLM + Vision Expert & 77.4 & 88.2 \\
VLM + Action Expert & 75.1 & 90.4 \\
Full VLM + Vision Expert + Action Expert & 84.6 & 95.8 \\
\hline
\end{tabular}
\end{table}

The full integrated configuration performs best, confirming that effective coordination
between perception, language grounding, and action generation is necessary for robust
robot manipulation.

The ablation trend also suggests complementarity: Vision Expert primarily improves
scene grounding, while Action Expert improves executability; using both yields the
largest improvement in task success and action validity.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{DiT_attn_heatmap.png}
    \caption{Action-token to context-token attention map in the final diffusion transformer block. Higher attention indicates stronger conditioning signals used for action generation.}
    \label{fig:dit_attention_heatmap}
\end{figure}

As shown in Figure~\ref{fig:dit_attention_heatmap}, attention is not uniform across
context tokens. The model repeatedly emphasizes a compact set of context positions,
with additional vertical peaks distributed across the sequence, indicating that
action denoising uses both persistent global context and sparse task-relevant cues.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{raw_qwen_img_token.png}
    \caption{Qwen image-token activation maps across transformer depth for task-focused scene understanding.}
    \label{fig:qwen_activation_depth}
\end{figure}

Figure~\ref{fig:qwen_activation_depth} indicates a depth-wise transition from
locally concentrated activations in early layers to broader scene-level responses in
deeper layers. This supports the integration hypothesis that the visual backbone
provides both local object cues and global layout cues to downstream action modules.


%─────────────────────────────────────────────────────────────────────────────
\section{NATURAL-LANGUAGE INSTRUCTION FOLLOWING}
%─────────────────────────────────────────────────────────────────────────────

The system is tested with varied instruction styles, including short commands,
compound instructions, and rephrased requests.

This section evaluates linguistic robustness independently from visual-domain shift.

\begin{table}[ht]
\centering
\caption{Natural-language instruction-following performance.}
\label{tab:language_following}
\begin{tabular}{lcc}
\hline
\textbf{Instruction Type} & \textbf{Success (\%)} & \textbf{Notes} \\
\hline
Direct imperative commands & 90.2 & Most reliable case \\
Rephrased conversational commands & 83.7 & Robust to wording variation \\
Multi-step language instructions & 79.8 & Errors mainly in step ordering \\
\hline
\end{tabular}
\end{table}

Results show that the proposed VLA system can follow natural-language instructions
reliably, with strongest performance on direct and moderately complex commands.

The performance drop on multi-step language instructions is consistent with accumulated
planning uncertainty across longer horizons, especially when step ordering must be
inferred from implicit language cues.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Qwen_context_embeding_cluster.png}
    \caption{Context-embedding clustering before and after projection, showing improved separation by task type while preserving object-color structure.}
    \label{fig:qwen_embedding_clusters}
\end{figure}

In Figure~\ref{fig:qwen_embedding_clusters}, task-type clusters (Place vs Reach)
are more separable after projection than in the raw embedding space, while color
groupings remain distributed. This suggests the projected representation is more
task-discriminative and less dominated by superficial color grouping, which aligns
with robust instruction-following under varied wording and scenes.


%─────────────────────────────────────────────────────────────────────────────
\section{GENERALIZATION AND ZERO-SHOT PERFORMANCE}
%─────────────────────────────────────────────────────────────────────────────

Generalization is measured by holding out unseen task combinations and object
arrangements during training.

The transfer effect is summarized by the unseen-to-seen ratio:
\[
\frac{78.6}{87.9} \times 100 \approx 89.4\%.
\]

\begin{table}[ht]
\centering
\caption{Seen vs unseen task performance.}
\label{tab:generalization_results}
\begin{tabular}{lcc}
\hline
\textbf{Split} & \textbf{Task Success (\%)} & \textbf{Action Validity (\%)} \\
\hline
Seen tasks & 87.9 & 96.4 \\
Unseen tasks & 78.6 & 92.1 \\
\hline
\end{tabular}
\end{table}

Although unseen-task performance is lower than seen-task performance, the model
retains strong action validity and acceptable task completion, indicating practical
generalization beyond memorized templates.

This result is particularly relevant for low-resource development because it shows that
generalization is retained without scaling dataset duration to large-collection
regimes.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{auto_reg_chucking_proof.png}
    \caption{Autoregressive chunking consistency in overlap regions, demonstrating stable joint prediction across chunk boundaries.}
    \label{fig:autoregressive_chunking}
\end{figure}

Figure~\ref{fig:autoregressive_chunking} shows near-overlapping trajectories in the
two highlighted overlap windows ($t=40$ to $60$ and $t=80$ to $100$), indicating
that chunk transitions do not introduce large discontinuities. This supports the
claim that the decoding strategy generalizes to longer horizons without visible seam
artifacts at chunk boundaries.


%─────────────────────────────────────────────────────────────────────────────
\section{TASK-WISE MANIPULATION PERFORMANCE}
%─────────────────────────────────────────────────────────────────────────────

Category-level results are summarized below.

The category view complements aggregate metrics by identifying which manipulation
patterns dominate residual error.

\begin{table}[ht]
\centering
\caption{Performance by manipulation task category.}
\label{tab:task_category_results}
\begin{tabular}{lcc}
\hline
\textbf{Task Category} & \textbf{Success (\%)} & \textbf{Action Validity (\%)} \\
\hline
Pick-and-place & 89.4 & 97.2 \\
Stacking & 82.1 & 94.5 \\
Drawer/container interaction & 86.3 & 95.1 \\
Multi-step sequencing & 80.5 & 93.0 \\
\hline
\end{tabular}
\end{table}

Multi-step sequencing remains the most challenging case due to long-horizon
instruction grounding, but all categories show stable executable outputs.

The gap between sequencing and simpler tasks suggests that future improvements should
prioritize temporal planning structure rather than command formatting, which is already
highly stable.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{trajectory_ttrd.png}
    \caption{Trajectory segmentation under a motion threshold, illustrating moving and stationary phases used for temporal behavior analysis.}
    \label{fig:trajectory_threshold_segments}
\end{figure}

From Figure~\ref{fig:trajectory_threshold_segments}, the thresholding process
separates active motion phases from quasi-static holding phases. Several segments
show low average speed and near-flat joint traces, while high-speed segments capture
repositioning and transport behaviors. This segmentation helps explain why
multi-step tasks are harder: errors accumulate during longer moving segments.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{joint_distributions.png}
    \caption{Per-joint position and anchor-velocity distributions used to inspect action-space coverage and motion statistics.}
    \label{fig:joint_distributions}
\end{figure}

Figure~\ref{fig:joint_distributions} reveals strong anisotropy across joints.
Joint 6 (gripper) is clearly bimodal in absolute position, consistent with open/close
behavior, and has the largest velocity spread. In contrast, Joint 4 has a narrow
velocity distribution, indicating a mostly stabilizing role during many episodes.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{joit_pos_cor_matrix.png}
    \caption{Joint position correlation matrix computed from the LeRobot dataset split.}
    \label{fig:joint_corr_lerobot}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{joit_pos_cor_matrix_not_lerobot.png}
    \caption{Joint position correlation matrix computed from the project evaluation set.}
    \label{fig:joint_corr_evalset}
\end{figure}

Comparing Figures~\ref{fig:joint_corr_lerobot} and~\ref{fig:joint_corr_evalset},
the dominant anti-correlation between Joint 2 and Joint 3 remains stable
($\approx -0.87$), suggesting consistent kinematic coupling across datasets.
However, correlations involving Joint 6 shift noticeably, indicating that evaluation
tasks induce different gripper-arm coordination patterns than the source dataset.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{per_ep_joint_pos.png}
    \caption{Per-episode joint position range heatmap indicating variability by joint and episode.}
    \label{fig:per_episode_joint_range}
\end{figure}

Figure~\ref{fig:per_episode_joint_range} further confirms that Joint 6 has the
largest per-episode range, while Joint 5 remains comparatively low-variance. This
supports the interpretation that grasp state changes are the primary source of
high-amplitude variation in many tasks.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{end_eff_space_cover.png}
    \caption{End-effector workspace coverage in 3D and projected density views, indicating explored reachable regions.}
    \label{fig:end_effector_workspace}
\end{figure}

As illustrated in Figure~\ref{fig:end_effector_workspace}, explored end-effector
poses occupy a bounded region with dense concentration along a narrow subset of the
reachable space. This indicates stable repeatable execution for benchmark tasks, but
also suggests that broader workspace exploration data may improve out-of-distribution
generalization.


%─────────────────────────────────────────────────────────────────────────────
\section{DESIGN CONSTRAINT VALIDATION}
%─────────────────────────────────────────────────────────────────────────────

\subsection{Computation and Memory}

The selected model configuration runs within the available GPU memory budget during
inference and supports practical deployment without offloading critical steps to
external services.

This confirms that the architecture remains feasible in the targeted 8 GB VRAM class
deployment setting.

\subsection{Latency}

Median end-to-end latency is measured at 1.84 seconds per command cycle, meeting the
real-time target for interactive manipulation.

Although not ultra-low-latency, this level is sufficient for supervised interactive
operation and instruction-driven manipulation workflows.

\subsection{Integration Efficiency}

The module interface design avoids major synchronization overhead. Integration remains
stable under continuous command cycles and mixed instruction complexity.

This validates the design decision to use explicit module interfaces plus a deterministic
TCP/IP adapter.


%─────────────────────────────────────────────────────────────────────────────
\section{VLM INTERPRETABILITY SUB-PROJECT RESULTS}
\label{sec:vlm_interp_results}
%─────────────────────────────────────────────────────────────────────────────

\subsection{Dataset and Corpus}

The pipeline generated 10\,000 JSONL records from EO-Data1.5M, covering all four
task categories. Activation harvesting over 100\,000 samples produced up to
12.8\,million layer-20 vectors across 100 shards. All three SAE checkpoints
(\texttt{sae\_task\_model.pt}, \texttt{sae\_agentic\_model.pt},
\texttt{sae\_topk\_production.pt}) trained without instability; the TopK
constraint ($k=32$) maintained constant sparsity throughout, avoiding the
active-feature collapse that $L_1$-penalized SAEs can exhibit.

\subsection{Feature Discovery}

Cosine-similarity probing on \texttt{sae\_agentic\_model.pt} with the anchor
prefix \texttt{call\_robot(`pick a} consistently placed \textbf{Feature \#5086}
in the top-3 across multiple scene images and phrasings. Max-activation scanning
of 5\,000 samples confirmed the label: every top-5 activating prompt was a
multi-step object-handling request (e.g., ``grab the banana and place it in the
bag''). No descriptive or QA prompts appeared, confirming Feature \#5086 encodes
\emph{agentic task planning} specifically. A parallel analysis on the production
checkpoint identified Feature \#9622 as a broader ``Procedural Robotic Planning''
direction activating on any \texttt{call\_robot()} context, complementary to
\#5086.

\subsection{Activation Steering}

Feature \#5086 was swept across $\alpha \in \{0, 2, 5, 10, 20, 50, 80\}$ using two
parallel prompts: a general knowledge question and a robot trajectory request.
Table~\ref{tab:steering_sweep} summarises the behavioral progression.

\begin{table}[ht]
\centering
\caption{Output behavior of Feature \#5086 across injection strengths. Both a
general-knowledge prompt and a robot-trajectory prompt were tested simultaneously.}
\label{tab:steering_sweep}
\begin{tabular}{cp{5.2cm}p{5.2cm}}
\hline
\textbf{$\alpha$} & \textbf{Robot Prompt} & \textbf{General Prompt} \\
\hline
0  & Free-form natural language; no tool-call structure.
   & Normal factual answer (e.g., ``Paris''). \\
2  & Occasional \texttt{call\_robot()} fragments appear.
   & Largely unaffected. \\
5  & Consistent \texttt{call\_robot()} prefix; no coordinates yet.
   & Mild drift toward spatial language. \\
10 & Structured action sequence with object specification.
   & Factual accuracy begins to degrade. \\
20 & High-specificity coordinate sequence; best task alignment.
   & Robotic format imposed; factual answer lost. \\
50 & Structure maintained; coordinate precision degrades.
   & Fully overridden; response incoherent as QA. \\
80 & Repetition penalty suppresses loops; coherence degrades.
   & Same as $\alpha=50$ with higher degeneration risk. \\
\hline
\end{tabular}
\end{table}

The steering effect is task-conditioned at $\alpha \leq 5$ and unconditioned at
$\alpha \geq 20$. The safe operating range is $\alpha \in [10, 20]$. Multi-turn
validation at $\alpha=15$ confirmed context retention across a three-step grasp
sequence (approach $\to$ verify $\to$ place) without restating the scene. All four
manipulation categories produced valid \texttt{call\_robot()} outputs, showing
Feature \#5086 is task-type-agnostic. Current results are observational; end-to-end
execution on the physical robot remains as future work.


%─────────────────────────────────────────────────────────────────────────────
\section{DISCUSSION}
%─────────────────────────────────────────────────────────────────────────────

The experiments confirm the five core objectives. First, an end-to-end VLA system can
be built for practical manipulation. Second, explicit integration of VLM, Vision
Expert, and Action Expert provides clear benefits over partial pipelines. Third,
natural-language control is feasible for non-programmatic interaction. Fourth,
meaningful generalization to unseen tasks is achieved, though with expected
performance drop compared to seen tasks. Fifth, the system performs robustly across
multiple manipulation categories while satisfying real-time constraints.

The main residual weakness is long-horizon sequencing in unseen contexts. This
suggests future gains from richer multi-step demonstrations and stronger temporal
reasoning constraints in the Action Expert.

From a research perspective, the chapter supports three broader conclusions.
\begin{enumerate}[label=\arabic*)]
    \item \textbf{Practicality under constraints}: VLA-based manipulation can be
    implemented with constrained data and commercial hardware when interface design and
    action validation are explicit.
    \item \textbf{Integration over isolated strength}: coordinated multimodal-action
    integration yields greater robustness than relying on a single dominant module.
    \item \textbf{Transfer as a primary criterion}: unseen-task retention provides a
    more informative deployment signal than seen-task accuracy alone in low-resource
    settings.
\end{enumerate}

\clearpage
